# Simple NodeJS web job for Azure

The web job outputs default environment variables and configuration options for web job run on default instance of Azure web app (site).

## Author
@peterblazejewicz